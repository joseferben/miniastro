---
import Layout from "@/layouts/Layout.astro";
import { AppPage } from "@/components/app-page/page";

export const prerender = false;

if (!Astro.locals.session) {
  return Astro.redirect("/");
}
---

<Layout>
  <AppPage client:only="react" />
</Layout>
